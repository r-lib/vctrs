# vctrs methods for data frames

These functions help the base data.frame class fit into the vctrs type
system by providing coercion and casting functions.

## Usage

``` r
# S3 method for class 'data.frame'
vec_ptype2(x, y, ...)

# S3 method for class 'data.frame'
vec_cast(x, to, ...)
```
